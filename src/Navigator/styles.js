import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headerItems: {
    padding: 15,
  },
  containerImage: {
    flex: 1,
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
  },
  image: {
    height: 200,
    width: 200,
    resizeMode: "cover",
  },
});
