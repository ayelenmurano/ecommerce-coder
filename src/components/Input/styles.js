import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    flex: 4,
  },
  input: {
    paddingHorizontal: 10,
    paddingVertical: 5,
    borderColor: "#9b9b9b",
    borderWidth: 1,
    height: 40,
    borderRadius: 6,
  }
});
